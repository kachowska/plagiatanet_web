# Оптимизация критического пути загрузки

## Исправленные критические проблемы

### 🔴 Конфликт версий React в import map
**Проблема:** Import map содержал два конфликтующих источника для React:
- esm.sh с React 18.2.0 (точные импорты)
- aistudiocdn с React 19.2.0 (prefix импорты)

**Последствия:** Загрузка двух версий React → Hooks errors, Context breakage, увеличенный размер бандла

**Решение:** Унифицирована версия React 19.2.0 через esm.sh, удалены конфликтующие prefix маппинги

См. подробности: [VERIFY_REACT_VERSIONS.md](./VERIFY_REACT_VERSIONS.md)

## Внесённые изменения

### Понимание Resource Hints

| Директива | Что делает | Когда использовать | Пример |
|-----------|------------|-------------------|--------|
| **preload** | Загружает ресурс с высоким приоритетом | Критические локальные ресурсы | `<link rel="preload" href="/app.css" as="style">` |
| **modulepreload** | Предзагружает ES модули | ES6 модули и их зависимости | `<link rel="modulepreload" href="/app.js">` |
| **preconnect** | DNS + TCP + TLS соединение | Критические внешние домены | `<link rel="preconnect" href="https://api.example.com">` |
| **dns-prefetch** | Только DNS резолюция | Фоллбэк для старых браузеров | `<link rel="dns-prefetch" href="https://fonts.googleapis.com">` |

### 1. Resource Hints (index.html)

**Для локальных критических ресурсов:**
✅ **Встроенные критические стили** - в `<style>` теге для мгновенного применения
✅ **fetchpriority="high"** для главного модуля - браузер загружает с высоким приоритетом

**Для внешних доменов:**
✅ **Preconnect** для критических доменов:
- `fonts.googleapis.com` и `fonts.gstatic.com` - ранее подключение к Google Fonts
- `cdn.tailwindcss.com` - ранее подключение к Tailwind CDN
- `esm.sh` и `aistudiocdn.com` - для React модулей

✅ **DNS-prefetch** как фоллбэк для старых браузеров (fonts, tailwind)

### 2. Приоритизация загрузки
✅ **`fetchpriority="high"`** добавлен для:
- Tailwind CSS (критический для стилей)
- Главного модуля приложения (index.tsx)

### 3. Оптимизация шрифтов
✅ `display=swap` в Google Fonts URL - предотвращает FOIT
✅ Синхронная загрузка для быстрого применения

### 4. Vite Build оптимизации
✅ **Code splitting**:
- Vendor chunk для React библиотек (лучшее кэширование)
- CSS code splitting

✅ **Минификация**:
- Terser для уменьшения размера
- Удаление console.log/debug/info в production
- Сохранение console.error/warn для отладки production ошибок

## Ожидаемые результаты

| Метрика | До | После |
|---------|-----|-------|
| Критический путь | 308 мс | ~200 мс |
| Количество запросов | 5 | 5 (оптимизирован порядок) |
| Размер бандла | 95.3 КБ | ~70 КБ (после minification) |
| Preconnect источники | 0 | 6 |

## Дополнительные рекомендации

### Краткосрочные (можно сделать сейчас):
1. **Рассмотреть замену Tailwind CDN** на локальный build - уменьшит размер с 124 КБ до ~10-20 КБ
2. **Добавить Service Worker** для кэширования ресурсов
3. **Использовать HTTP/2 Server Push** для критических ресурсов

### Среднесрочные:
1. **Lazy loading** для некритичных компонентов (Blog, Testimonials)
2. **Динамический импорт** для модальных окон
3. **Оптимизация изображений** - использовать WebP/AVIF форматы

### Долгосрочные:
1. **Миграция с Tailwind CDN** на PostCSS build
2. **Внедрение SSG/SSR** для улучшения First Contentful Paint
3. **CDN** для статических ресурсов

## Мониторинг

После деплоя проверьте:
- PageSpeed Insights: https://pagespeed.web.dev/
- WebPageTest: https://www.webpagetest.org/
- Chrome DevTools Performance panel

Целевые метрики:
- LCP < 2.5s ✅
- FID < 100ms ✅
- CLS < 0.1 ✅
- Critical Request Chains < 200ms 🎯

